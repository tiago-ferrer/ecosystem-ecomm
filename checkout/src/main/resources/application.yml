server:
   port: 8082
spring:
  application:
    name: checkout
  datasource:
    url: jdbc:postgresql://postgres-checkout:5433/checkout_db
    username: user
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
     username: guest
     password: guest
     host: rabbitmq
     port: 5672

  cloud:
     stream:
       bindings:
         checkoutSupplier-out-0:
           destination: app.v1.event.checkout

