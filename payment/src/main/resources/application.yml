server:
  port:8083
spring:
  application:
    name: checkout
  datasource:
    url: ${db.uri.auth.checkout}
    username: ${db.username.auth}
    password: ${db.password.auth}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    username: {rabbitmq.username}
    password: {rabbitmq.password}
    host: {rabbitmq.host}
    port: 5672

  cloud:
    stream:
      function:
        definition: checkoutEventListener
        bindings:
          checkoutEventListener-in-0:
            destination: app.v1.event.checkout
            group: checkout

  api:
    security:
      token: b647323e4680065537924fb9876da1f0fd71f6558640e847dd8ea39c72a8ee46
      group: Grupo10
      members:
        - Rafael Amaral
        - André S Ferreira
        - Giulliana Munhoz
        - Tiago Santana
        - Eduardo Vilhena